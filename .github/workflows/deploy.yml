name: 🚀 Deploy Valora.vip - Complete Platform

# Despliegue automático cuando hay cambios en main
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: 🌐 Deploy Complete Valora Platform
    
    steps:
    # 1. Checkout del código con toda la nueva funcionalidad
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    # 2. Setup PHP Environment
    - name: 🔍 Setup PHP Environment
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: mbstring, xml, ctype, iconv, intl, pdo, mysql, gd, curl
        
    # 3. Install Composer Dependencies
    - name: 📦 Install Dependencies
      run: |
        if [ -f composer.json ]; then
          composer install --no-dev --optimize-autoloader --no-interaction
          echo "✅ Composer dependencies installed"
        else
          echo "ℹ️ No composer.json found, skipping composer install"
        fi
        
    # 4. Validate PHP Syntax and System
    - name: 🧪 Validate Complete System
      run: |
        echo "🔍 Validando sintaxis PHP en todo el sistema..."
        find . -name "*.php" -not -path "./vendor/*" -exec php -l {} \;
        echo "✅ Validación de sintaxis completada"
        
        echo "📊 Estadísticas del sistema completo:"
        echo "📄 Archivos PHP: $(find . -name '*.php' -not -path './vendor/*' | wc -l)"
        echo "🤖 Sistema de IA: $(ls -la controllers/login/usernameGenerator.php 2>/dev/null && echo 'Presente' || echo 'No encontrado')"
        echo "🎯 Wizard IA: $(ls -la views/login/registranteUserAvailavilitySelect.php 2>/dev/null && echo 'Presente' || echo 'No encontrado')"
        echo "🔑 Sistema Auth: $(ls -la controllers/login/AuthController.php 2>/dev/null && echo 'Presente' || echo 'No encontrado')"
        echo "� Reset Password: $(ls -la controllers/login/PasswordResetController.php 2>/dev/null && echo 'Presente' || echo 'No encontrado')"
        echo "👥 Panel Admin: $(ls -la views/admin/ 2>/dev/null && echo 'Presente' || echo 'No encontrado')"
        
    # 5. Prepare Complete System for Deployment
    - name: 📁 Prepare Complete Platform
      run: |
        echo "📋 Sistema completo listo para deployment:"
        echo "🏠 Estructura principal:"
        ls -la
        echo ""
        echo "🎯 Controladores:"
        ls -la controllers/login/ 2>/dev/null || echo "⚠️ Directorio controllers/login no encontrado"
        echo ""
        echo "👁️ Vistas principales:"
        ls -la views/login/ 2>/dev/null || echo "⚠️ Directorio views/login no encontrado"
        echo ""
        echo "🔧 Panel administrativo:"
        ls -la views/admin/ 2>/dev/null || echo "⚠️ Directorio views/admin no encontrado"
        
        echo "🔍 Verificando estructura MVC:"
        echo "📁 Controllers: $(ls -1 controllers/ 2>/dev/null | wc -l) archivos"
        echo "📁 Models: $(ls -1 models/ 2>/dev/null | wc -l) archivos"  
        echo "📁 Views: $(ls -1 views/ 2>/dev/null | wc -l) archivos"
        echo "📁 Services: $(ls -1 services/ 2>/dev/null | wc -l) archivos"
        echo "📁 Config: $(ls -1 config/ 2>/dev/null | wc -l) archivos"
        
        echo "� Verificando sistema de email:"
        [ -f "services/EmailService.php" ] && echo "✅ EmailService.php encontrado" || echo "❌ EmailService.php faltante"
        [ -f "config/email-config.php" ] && echo "✅ email-config.php encontrado" || echo "❌ email-config.php faltante"
        [ -d "vendor" ] && echo "✅ Dependencias Composer encontradas" || echo "❌ Directorio vendor faltante"
        
    # 6. Debug - Verificar secretos (sin mostrar valores reales)
    - name: 🔍 Debug Connection Info
      run: |
        echo "🔍 Verificando configuración de deploy..."
        echo "📡 FTP Host configurado: $(echo '${{ secrets.FTP_HOST }}' | sed 's/./*/g')"
        echo "👤 FTP User configurado: $(echo '${{ secrets.FTP_USERNAME }}' | sed 's/./*/g')"  
        echo "🔐 FTP Pass configurado: $(if [ -n '${{ secrets.FTP_PASSWORD }}' ]; then echo '***SET***'; else echo '***NOT SET***'; fi)"
        echo "� FTP Port configurado: ${{ secrets.FTP_PORT || 21 }}"
        echo "�📁 Directorio destino: /public_html/"
        echo "🌐 Protocolo: FTP"
        
    # 7. Deploy Complete Platform via SFTP
    - name: 🚀 Deploy Complete Platform (SFTP)
      id: sftp-deploy
      continue-on-error: true
      uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: 22
        local_path: './*'
        remote_path: '/public_html/'
        sftp_only: true
        args: '-o ConnectTimeout=10'

    # 8. Verificación post-deployment
    - name: ✅ Post-Deployment System Check
      if: steps.sftp-deploy.outcome == 'success'
      run: |
        echo "🎉 ¡DEPLOYMENT EXITOSO!"
        echo "================================"
        echo "🌐 Sitio Web: https://valora.vip"
        echo "🔐 Login: https://valora.vip/views/login/login.php"
        echo "📝 Registro: https://valora.vip/views/login/register.php"
        echo "🔑 Reset Password: https://valora.vip/views/login/password_reset.php"
        echo "🔧 Panel Admin: https://valora.vip/views/admin/"
        echo "👥 Panel Permisos: https://valora.vip/views/admin/permissionsPanel.php"
        echo "🔍 System Check: https://valora.vip/views/checksTests/system-check.php"
        echo "================================"
        echo "✅ FUNCIONALIDADES DESPLEGADAS:"
        echo "✅ Sistema de autenticación completo"
        echo "✅ Panel de permisos con manejo robusto de errores"
        echo "✅ Toggle de módulos exentos"
        echo "✅ Sistema de tickets de soporte"
        echo "✅ Recuperación de contraseña"
        echo "✅ Panel administrativo"
        echo "================================"

    # 9. Manejar errores de deployment
    - name: ❌ Handle Deployment Failure
      if: steps.sftp-deploy.outcome == 'failure'
      run: |
        echo "❌ DEPLOYMENT FALLÓ"
        echo "================================"
        echo "🔍 Posibles problemas:"
        echo "- Verificar credenciales SFTP en GitHub Secrets"
        echo "- Confirmar que el servidor soporta SFTP (puerto 22)"
        echo "- Revisar permisos en /public_html/"
        echo "- Verificar que no hay archivos bloqueados"
        echo "================================"
        echo "🛠️ Para debug:"
        echo "1. Ir a GitHub Actions y revisar logs detallados"
        echo "2. Verificar secrets: FTP_HOST, FTP_USERNAME, FTP_PASSWORD"
        echo "3. Probar conexión SFTP con FileZilla (puerto 22)"
        echo "4. Considerar deployment manual"
        echo "================================"
        exit 1